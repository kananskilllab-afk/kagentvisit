---
phase: 02-action-item-tracker-backend-data-model
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - server/package.json
  - server/jest.config.js
  - server/tests/models/actionItem.model.test.js
  - server/tests/api/actionItems.api.test.js
  - server/tests/api/actionItems.auth.test.js
  - server/tests/jobs/overdueDigest.test.js
autonomous: true
requirements:
  - AIT-01
  - AIT-02
  - AIT-03
  - AIT-04
  - AIT-05

must_haves:
  truths:
    - "jest and supertest are present in server/node_modules after install"
    - "server/jest.config.js exists and node -e require('./server/jest.config.js') exits 0"
    - "Four skeleton test files exist with at least one describe/it block each"
    - "cd server && npx jest --listTests exits 0 and lists all 4 test files"
  artifacts:
    - path: "server/package.json"
      provides: "jest and supertest in devDependencies; test script in scripts"
      contains: "jest"
    - path: "server/jest.config.js"
      provides: "Jest configuration: testEnvironment node, testMatch for tests/ dir, forceExit"
      exports: ["module.exports"]
    - path: "server/tests/models/actionItem.model.test.js"
      provides: "Skeleton tests for AIT-01 (actionItems schema shape)"
      contains: "describe"
    - path: "server/tests/api/actionItems.api.test.js"
      provides: "Skeleton tests for AIT-02 and AIT-03 (CRUD endpoints + history + audit)"
      contains: "describe"
    - path: "server/tests/api/actionItems.auth.test.js"
      provides: "Skeleton tests for AIT-04 (authorization: 403 for wrong role)"
      contains: "describe"
    - path: "server/tests/jobs/overdueDigest.test.js"
      provides: "Skeleton tests for AIT-05 (cron digest grouping)"
      contains: "describe"
  key_links:
    - from: "server/jest.config.js"
      to: "server/tests/**/*.test.js"
      via: "testMatch pattern"
      pattern: "tests/.*\\.test\\.js"
    - from: "server/package.json scripts.test"
      to: "server/jest.config.js"
      via: "jest --config jest.config.js"
      pattern: "jest"
---

<objective>
Install the jest + supertest test framework and scaffold skeleton test files for all AIT requirements.

Purpose: Nyquist compliance requires every production code task to have an automated `<verify>` command that runs in under 60 seconds. Plans 02-01, 02-02, and 02-03 create the actionItems schema, REST controller, and cron job respectively. Without test files existing BEFORE those plans run, their `<automated>` verify blocks can only use `node -e require()` smoke tests — insufficient for behavioral assertions. This Wave 0 plan unblocks all three downstream waves by creating the test infrastructure and skeleton files first.

Output:
- jest + supertest installed as devDependencies in server/package.json
- server/jest.config.js with testEnvironment: node, forceExit: true
- 4 skeleton test files with describe/it structure — NOT yet implemented (that is Wave 1–3's job)
- `npm test` script added to server/package.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-action-item-tracker-backend-data-model/02-RESEARCH.md
@.planning/phases/02-action-item-tracker-backend-data-model/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install jest + supertest and create jest.config.js</name>
  <files>
    server/package.json
    server/jest.config.js
  </files>

  <read_first>
    - server/package.json — read in full; confirm jest and supertest are NOT already in devDependencies; identify the scripts block to add the test script
  </read_first>

  <action>
    ── Step 1: Install dependencies ──

    Run from the server directory:
    ```bash
    cd server && npm install --save-dev jest supertest
    ```

    This adds jest and supertest to the devDependencies section of server/package.json.

    ── Step 2: Add test script to server/package.json ──

    Read server/package.json. In the "scripts" block, add:
    ```json
    "test": "jest --config jest.config.js --forceExit"
    ```

    If a "test" script already exists, replace it with the above. Do NOT modify any other script entry.

    ── Step 3: Create server/jest.config.js ──

    Create the file server/jest.config.js with the following content:

    ```javascript
    'use strict';
    /** @type {import('jest').Config} */
    module.exports = {
        testEnvironment: 'node',
        testMatch: ['**/tests/**/*.test.js'],
        forceExit: true,
        // Increase timeout for integration tests that may await DB operations
        testTimeout: 15000,
        // Suppress noisy console output during test runs
        silent: false
    };
    ```

    Do NOT add transform, moduleNameMapper, or coverage settings — keep the config minimal.
  </action>

  <verify>
    <automated>cd "C:\Users\Kanan\OneDrive\Desktop\Projects\kagent_visit\kagentvisit\server" && node -e "const cfg=require('./jest.config.js'); console.log('testEnvironment:', cfg.testEnvironment); console.log('forceExit:', cfg.forceExit); const pkg=require('./package.json'); console.log('jest devDep:', !!pkg.devDependencies.jest); console.log('supertest devDep:', !!pkg.devDependencies.supertest); process.exit(cfg.testEnvironment==='node' && cfg.forceExit && pkg.devDependencies.jest && pkg.devDependencies.supertest ? 0 : 1)"</automated>
  </verify>

  <done>server/package.json has jest and supertest in devDependencies, a "test" script running jest. server/jest.config.js exists with testEnvironment: node and forceExit: true.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold 4 skeleton test files</name>
  <files>
    server/tests/models/actionItem.model.test.js
    server/tests/api/actionItems.api.test.js
    server/tests/api/actionItems.auth.test.js
    server/tests/jobs/overdueDigest.test.js
  </files>

  <action>
    Create the directory structure server/tests/models/, server/tests/api/, server/tests/jobs/ if not present, then create each file below.

    IMPORTANT: These are SKELETON files — all it() blocks must call test.todo() or be marked pending. Do NOT write actual assertions or mock implementations here. Wave 1–3 plans fill in the bodies. The purpose is to make `npx jest --listTests` return all 4 files and to establish the describe/it contract so plan verify blocks can reference named tests.

    ── server/tests/models/actionItem.model.test.js ──

    ```javascript
    'use strict';
    /**
     * AIT-01: actionItems schema shape tests
     * These tests verify the Mongoose schema added in Plan 02-01.
     * Skeleton only — bodies implemented after Wave 1 runs.
     */

    describe('ActionItem schema — Visit model', () => {
        it.todo('actionItems path resolves on visitSchema');
        it.todo('actionItemSchema has required text field');
        it.todo('actionItemSchema status defaults to "open"');
        it.todo('actionItemSchema status rejects invalid values');
        it.todo('actionItemHistorySchema change field has correct enum');
        it.todo('3 compound indexes are declared on visitSchema');
    });

    describe('ActionItem schema — VisitSchedule model', () => {
        it.todo('actionItems path resolves on visitScheduleSchema');
        it.todo('actionItems field defaults to []');
        it.todo('3 compound indexes are declared on visitScheduleSchema');
    });

    describe('AuditLog enum extension', () => {
        it.todo('targetModel enum includes "ActionItem"');
    });

    describe('Notification enum extension', () => {
        it.todo('type enum includes "action_item_overdue"');
        it.todo('visitRef field exists on notificationSchema');
    });
    ```

    ── server/tests/api/actionItems.api.test.js ──

    ```javascript
    'use strict';
    /**
     * AIT-02: Action item CRUD endpoint tests
     * AIT-03: History append + AuditLog write tests
     * These tests exercise the routes added in Plan 02-02.
     * Skeleton only — bodies implemented after Wave 2 runs.
     */

    describe('GET /api/visits/:id/action-items', () => {
        it.todo('returns 200 with actionItems array for existing visit');
        it.todo('returns 404 when visit does not exist');
        it.todo('returns 200 with empty array when visit has no action items');
    });

    describe('POST /api/visits/:id/action-items', () => {
        it.todo('returns 201 with new action item on valid request');
        it.todo('new item has status "open" and history[0].change === "created"');
        it.todo('returns 400 when text is missing');
        it.todo('returns 404 when visit does not exist');
    });

    describe('PUT /api/visits/:id/action-items/:itemId', () => {
        it.todo('returns 200 and updates text');
        it.todo('history gains one entry with change === "edited"');
        it.todo('returns 400 when text is missing');
        it.todo('returns 404 when item does not exist');
    });

    describe('PUT /api/visits/:id/action-items/:itemId/status', () => {
        it.todo('returns 200 and updates status');
        it.todo('history gains one entry with change === "status_changed"');
        it.todo('returns 400 for invalid status value');
    });

    describe('DELETE /api/visits/:id/action-items/:itemId', () => {
        it.todo('returns 200 and item is removed from actionItems[]');
        it.todo('AuditLog row with targetModel "ActionItem" exists after deletion');
    });

    describe('AuditLog writes (AIT-03)', () => {
        it.todo('createActionItem writes exactly one AuditLog row with action ACTION_ITEM_CREATE');
        it.todo('updateActionItem writes exactly one AuditLog row with action ACTION_ITEM_EDIT');
        it.todo('changeActionItemStatus writes one AuditLog row with action ACTION_ITEM_STATUS_CHANGE');
        it.todo('deleteActionItem writes AuditLog BEFORE pulling from array');
    });
    ```

    ── server/tests/api/actionItems.auth.test.js ──

    ```javascript
    'use strict';
    /**
     * AIT-04: Authorization tests for action item mutation
     * Verifies canMutateActionItem() guard behavior.
     * Skeleton only — bodies implemented after Wave 2 runs.
     *
     * Design decision (AIT-04):
     * "members of the agent's account team" is interpreted as admin/hod users
     * who have the visit's submittedBy user in their assignedEmployees array.
     * The `accounts` role is excluded — it is read-only across the app per PROJECT.md.
     * This matches the canAccessPlan() pattern in visitPlans.controller.js.
     */

    describe('canMutateActionItem — authorized roles', () => {
        it.todo('superadmin receives 200 on POST');
        it.todo('admin receives 200 on POST');
        it.todo('hod with visit submitter in assignedEmployees receives 200 on POST');
        it.todo('visit submitter (own visit) receives 200 on POST');
    });

    describe('canMutateActionItem — unauthorized roles', () => {
        it.todo('accounts role receives 403 on POST');
        it.todo('user role (not submitter) receives 403 on POST');
        it.todo('hod WITHOUT visit submitter in assignedEmployees receives 403 on POST');
        it.todo('unauthenticated request receives 401 (protect middleware)');
    });

    describe('GET /api/visits/:id/action-items — open to any authenticated user', () => {
        it.todo('accounts role receives 200 on GET (reads are unrestricted)');
        it.todo('user role receives 200 on GET');
    });
    ```

    ── server/tests/jobs/overdueDigest.test.js ──

    ```javascript
    'use strict';
    /**
     * AIT-05: Overdue digest cron job tests
     * Verifies grouping logic and notification emission in runOverdueDigest().
     * Skeleton only — bodies implemented after Wave 3 runs.
     *
     * Note on visitRef (by design):
     * The digest notification covers MULTIPLE visits per user (one notification per user,
     * not one per visit). Therefore visitRef is intentionally null on action_item_overdue
     * notifications — there is no single visit to reference. This is documented in
     * overdueActionItems.job.js with an inline comment.
     */

    describe('runOverdueDigest — grouping logic', () => {
        it.todo('emits ONE notification per user regardless of how many overdue items they have');
        it.todo('groups by assignee first, falls back to submittedBy when assignee is absent');
        it.todo('does not emit notifications when no overdue items exist');
        it.todo('handles both Visit and VisitSchedule overdue items in the same digest run');
    });

    describe('runOverdueDigest — notification shape', () => {
        it.todo('notification type is "action_item_overdue"');
        it.todo('notification title includes the overdue item count');
        it.todo('sendEmailFlag is false (in-app only)');
        it.todo('visitRef is null by design — digest covers multiple visits per user');
    });

    describe('scheduleOverdueDigest — registration', () => {
        it.todo('scheduleOverdueDigest registers a cron task without throwing');
        it.todo('exported function is typeof function');
    });
    ```

    After creating all 4 files, run:
    ```bash
    cd server && npx jest --listTests --config jest.config.js
    ```
    Confirm all 4 files appear in the output.
  </action>

  <verify>
    <automated>cd "C:\Users\Kanan\OneDrive\Desktop\Projects\kagent_visit\kagentvisit\server" && npx jest --listTests --config jest.config.js --forceExit 2>&1 | tee /dev/null && node -e "const fs=require('fs'); const files=['tests/models/actionItem.model.test.js','tests/api/actionItems.api.test.js','tests/api/actionItems.auth.test.js','tests/jobs/overdueDigest.test.js']; const missing=files.filter(f=>!fs.existsSync(f)); if(missing.length){console.error('Missing:', missing); process.exit(1);} console.log('All 4 test files exist'); process.exit(0)"</automated>
  </verify>

  <done>4 skeleton test files exist under server/tests/ with describe/it.todo blocks. npx jest --listTests lists all 4. No assertions are implemented yet — those are filled in by Wave 1–3 executors as they implement each plan.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
cd "C:\Users\Kanan\OneDrive\Desktop\Projects\kagent_visit\kagentvisit\server" && node -e "
const fs = require('fs');
const pkg = require('./package.json');
const cfg = require('./jest.config.js');

const checks = [
  ['jest in devDependencies',     !!pkg.devDependencies?.jest],
  ['supertest in devDependencies', !!pkg.devDependencies?.supertest],
  ['test script present',         !!pkg.scripts?.test],
  ['jest.config testEnvironment', cfg.testEnvironment === 'node'],
  ['jest.config forceExit',       cfg.forceExit === true],
  ['model test file exists',      fs.existsSync('tests/models/actionItem.model.test.js')],
  ['api test file exists',        fs.existsSync('tests/api/actionItems.api.test.js')],
  ['auth test file exists',       fs.existsSync('tests/api/actionItems.auth.test.js')],
  ['jobs test file exists',       fs.existsSync('tests/jobs/overdueDigest.test.js')],
];

let pass = true;
checks.forEach(([name, ok]) => {
  console.log((ok ? '✓' : '✗') + ' ' + name);
  if (!ok) pass = false;
});
process.exit(pass ? 0 : 1);
"
```

All 9 checks must print `✓` and process must exit 0.

Then confirm jest can discover the files:
```bash
cd "C:\Users\Kanan\OneDrive\Desktop\Projects\kagent_visit\kagentvisit\server" && npx jest --listTests --config jest.config.js --forceExit
```
Expected: 4 paths printed, no errors.
</verification>

<success_criteria>
- jest and supertest appear in server/package.json devDependencies
- server/package.json scripts.test runs jest
- server/jest.config.js exists with testEnvironment: node, forceExit: true
- All 4 skeleton test files exist under server/tests/ with describe/it.todo blocks
- npx jest --listTests discovers all 4 files
- npx jest --forceExit exits 0 (all tests are .todo — no failures expected)
</success_criteria>

<output>
After completion, create `.planning/phases/02-action-item-tracker-backend-data-model/02-00-SUMMARY.md` with:
- jest and supertest versions installed
- jest.config.js settings
- 4 test file paths created
- Any deviations from the plan
</output>
