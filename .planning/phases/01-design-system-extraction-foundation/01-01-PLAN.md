---
phase: 01-design-system-extraction-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/index.html
  - client/tailwind.config.js
autonomous: true
requirements: [DS-01, DS-02, DS-03]
must_haves:
  truths:
    - "Inter and Manrope fonts load on every page without per-page imports"
    - "Tailwind utility classes like `bg-meridian-navy`, `text-meridian-text`, `shadow-meridian-card`, `font-display` resolve correctly"
    - "Existing `brand-*` and `kanan-*` Tailwind classes still work"
  artifacts:
    - path: "client/index.html"
      provides: "Google Fonts preconnect + Inter (300-800) + Manrope (500-900) stylesheet links"
      contains: "fonts.googleapis.com"
    - path: "client/tailwind.config.js"
      provides: "meridian.* color namespace, shadow-meridian-card, font-display"
      contains: "meridian:"
  key_links:
    - from: "client/index.html"
      to: "Google Fonts CDN"
      via: "preconnect + stylesheet"
      pattern: "rel=\"stylesheet\".*Inter.*Manrope|rel=\"stylesheet\".*Manrope.*Inter"
    - from: "client/tailwind.config.js"
      to: "Tailwind utility generator"
      via: "theme.extend.colors.meridian + theme.extend.fontFamily.display + theme.extend.boxShadow.meridian-card"
      pattern: "meridian:\\s*\\{"
---

<objective>
Wire the Meridian visual foundation into the existing build: load Inter + Manrope from Google Fonts (app-wide) and extend `client/tailwind.config.js` with the `meridian.*` color namespace, the `shadow-meridian-card` shadow, and the `font-display` (Manrope-first) family. No primitive components yet — this plan ONLY produces foundation tokens that subsequent plans consume.

Purpose: Every following plan / phase composes UI from `bg-meridian-*`, `text-meridian-*`, `shadow-meridian-card`, and `font-display` utility classes. They must resolve before primitives are written.

Output: Updated `client/index.html` with Google Fonts wired, and updated `client/tailwind.config.js` with `meridian.*` color keys, `meridian-card` shadow, and `display` font family. Existing `brand-*` / `kanan-*` keys preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-extraction-foundation/01-CONTEXT.md
@newui/meridian/shared.jsx
@client/index.html
@client/tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Google Fonts (Inter + Manrope) to client/index.html</name>
  <files>client/index.html</files>
  <read_first>
    - client/index.html (current head structure — must preserve favicon, viewport, charset, title)
    - .planning/phases/01-design-system-extraction-foundation/01-CONTEXT.md (Fonts LOCKED section — exact weights and preconnect rules)
  </read_first>
  <action>
    Edit `client/index.html` and add the following three `<link>` tags inside `<head>`, AFTER the existing `<meta name="viewport"...>` line and BEFORE the `<title>` line. Do NOT remove or modify any existing tags.

    Exact lines to insert:

    ```html
      <link rel="preconnect" href="https://fonts.googleapis.com" />
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
      <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Manrope:wght@500;600;700;800;900&display=swap" />
    ```

    Notes:
    - Inter weights: 300, 400, 500, 600, 700, 800 (per CONTEXT.md Fonts LOCKED).
    - Manrope weights: 500, 600, 700, 800, 900 (per CONTEXT.md Fonts LOCKED).
    - `display=swap` so text renders with fallback font while web font loads (no FOIT).
    - Use latest Google Fonts CSS2 endpoint (`/css2`); do not pin font versions — Google manages this per CONTEXT.md.
    - The `crossorigin` attribute on the `fonts.gstatic.com` preconnect is REQUIRED (font files are CORS-restricted).
    - Do NOT touch `<link rel="icon" ...>`, `<title>`, or the `<body>` content.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('client/index.html','utf8');const ok = /preconnect.*fonts\.googleapis\.com/.test(h) && /preconnect.*fonts\.gstatic\.com.*crossorigin|crossorigin.*fonts\.gstatic\.com/i.test(h) && /stylesheet.*Inter.*wght@300;400;500;600;700;800.*Manrope.*wght@500;600;700;800;900/.test(h) && /href=\"\/fav\.png\"/.test(h) && /AVS - Kanan International/.test(h); if(!ok){console.error('FAIL: index.html missing required Google Fonts wiring or destroyed existing tags');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `client/index.html` contains the literal substring `fonts.googleapis.com`
    - `client/index.html` contains the literal substring `fonts.gstatic.com` with `crossorigin` on the same tag
    - `client/index.html` contains a `<link rel="stylesheet"` whose `href` includes BOTH `Inter:wght@300;400;500;600;700;800` AND `Manrope:wght@500;600;700;800;900`
    - `client/index.html` still contains `<link rel="icon" type="image/png" href="/fav.png" />` (favicon preserved)
    - `client/index.html` still contains `<title>AVS - Kanan International</title>` (title preserved)
    - `client/index.html` still contains `<div id="root"></div>` and `<script type="module" src="/src/main.jsx"></script>` (root + entry preserved)
  </acceptance_criteria>
  <done>Google Fonts (Inter 300-800 + Manrope 500-900) wired in `client/index.html` head, with preconnect for both domains, without disturbing existing favicon/viewport/title/root tags. (Addresses DS-01.)</done>
</task>

<task type="auto">
  <name>Task 2: Extend client/tailwind.config.js with meridian.* tokens, font-display family, and shadow-meridian-card</name>
  <files>client/tailwind.config.js</files>
  <read_first>
    - client/tailwind.config.js (current `theme.extend` shape — must preserve `brand`, `kanan`, existing `boxShadow`, `backgroundImage`, `animation`, `keyframes`, existing `fontFamily.sans`)
    - newui/meridian/shared.jsx (lines 1-18: `M` object with exact hex values for navy/blue/gold/green/red/sky/purple/bg/white/border/text/sub/muted/rowHov + `cardShadow`)
    - .planning/phases/01-design-system-extraction-foundation/01-CONTEXT.md (Color palette LOCKED + Shadows / radius / typography LOCKED + Fonts LOCKED)
  </read_first>
  <action>
    Edit `client/tailwind.config.js` to add Meridian tokens to `theme.extend`. Do NOT remove or rename any existing keys (`brand`, `kanan`, `card`, `card-hover`, `glass`, `sidebar`, `nav`, `glow`, `brand-gradient`, `gold-gradient`, `soft-mesh`, `fade-in`, `slide-up`, `slide-down`, `pulse-slow`, `fadeIn`, `slideUp`, `slideDown`, `sans` font family). Existing pages depend on them.

    Make exactly these three additions:

    1. Inside `theme.extend.colors`, add a `meridian` sibling next to `brand` and `kanan`:

    ```js
    meridian: {
      navy:   '#1E1B4B',
      blue:   '#3B82F6',
      gold:   '#F59E0B',
      green:  '#10B981',
      red:    '#EF4444',
      sky:    '#0EA5E9',
      purple: '#8B5CF6',
      bg:     '#F1F5F9',
      white:  '#FFFFFF',
      border: '#E2E8F0',
      text:   '#0F172A',
      sub:    '#64748B',
      muted:  '#94A3B8',
      'row-hov': '#F8FAFC',
    },
    ```

    Naming note: CONTEXT.md uses `rowHov` in the JS `M` object but the Tailwind utility name MUST be kebab-case `row-hov` (so `bg-meridian-row-hov` works).

    2. Inside `theme.extend.fontFamily`, ADD a `display` key (keep existing `sans`):

    ```js
    fontFamily: {
        sans: ['Inter', 'system-ui', '-apple-system', 'sans-serif'],
        display: ['Manrope', 'Inter', 'system-ui', 'sans-serif'],
    },
    ```

    3. Inside `theme.extend.boxShadow`, ADD a `meridian-card` key (keep existing `card`, `card-hover`, `glass`, `sidebar`, `nav`, `glow`):

    ```js
    'meridian-card': '0 1px 3px rgba(0,0,0,0.06), 0 1px 2px rgba(0,0,0,0.04)',
    ```

    Decision being recorded (per CONTEXT.md "Claude's Discretion"): use class name `font-display` (idiomatic Tailwind) rather than `font-manrope`. Document this decision in the SUMMARY.md.

    Do NOT add custom radius keys — CONTEXT.md confirms Tailwind defaults (`rounded-md=6px`, `rounded-lg=8px`) match Meridian usage.

    Do NOT remove `plugins: []` or change `content`.
  </action>
  <verify>
    <automated>node -e "const c=require('./client/tailwind.config.js').default;const C=c.theme.extend.colors;const F=c.theme.extend.fontFamily;const S=c.theme.extend.boxShadow;const m=C.meridian;const checks=[['meridian colors present',!!m],['navy hex',m&&m.navy==='#1E1B4B'],['blue hex',m&&m.blue==='#3B82F6'],['gold hex',m&&m.gold==='#F59E0B'],['green hex',m&&m.green==='#10B981'],['red hex',m&&m.red==='#EF4444'],['sky hex',m&&m.sky==='#0EA5E9'],['purple hex',m&&m.purple==='#8B5CF6'],['bg hex',m&&m.bg==='#F1F5F9'],['white hex',m&&m.white==='#FFFFFF'],['border hex',m&&m.border==='#E2E8F0'],['text hex',m&&m.text==='#0F172A'],['sub hex',m&&m.sub==='#64748B'],['muted hex',m&&m.muted==='#94A3B8'],['row-hov hex',m&&m['row-hov']==='#F8FAFC'],['brand preserved',!!C.brand&&C.brand.navy==='#1E1B4B'],['kanan preserved',!!C.kanan],['font-display Manrope first',Array.isArray(F.display)&&F.display[0]==='Manrope'],['font-sans preserved',Array.isArray(F.sans)&&F.sans[0]==='Inter'],['shadow meridian-card',S['meridian-card']&&S['meridian-card'].includes('rgba(0,0,0,0.06)')],['shadow card preserved',!!S.card]];let bad=checks.filter(c=>!c[1]);if(bad.length){console.error('FAIL',bad.map(b=>b[0]));process.exit(1)}console.log('OK',checks.length,'checks passed')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "meridian:" client/tailwind.config.js` returns >= 1 (meridian color key exists)
    - `grep -c "'#1E1B4B'" client/tailwind.config.js` returns >= 2 (one for `brand.navy`, one for `meridian.navy`)
    - `grep -c "'meridian-card'" client/tailwind.config.js` returns 1
    - `grep -c "display: \\['Manrope'" client/tailwind.config.js` returns 1
    - `grep -c "brand:" client/tailwind.config.js` returns 1 (existing `brand` namespace preserved)
    - `grep -c "kanan:" client/tailwind.config.js` returns 1 (existing `kanan` namespace preserved)
    - `grep -c "'card':" client/tailwind.config.js` returns 1 (existing `card` shadow preserved — distinct from `meridian-card`)
    - Running `cd client && npx tailwindcss -i ./src/index.css -o /tmp/mer-test.css --content "./src/test-mer.html"` (with a temp HTML file containing `<div class="bg-meridian-navy text-meridian-text shadow-meridian-card font-display">x</div>`) succeeds without "Class does not exist" warnings
  </acceptance_criteria>
  <done>`client/tailwind.config.js` exposes the full Meridian token surface (`meridian.navy/blue/gold/green/red/sky/purple/bg/white/border/text/sub/muted/row-hov`, `font-display`, `shadow-meridian-card`) while preserving every existing `brand-*`, `kanan-*`, font, shadow, gradient, and animation key. (Addresses DS-02 + DS-03.)</done>
</task>

</tasks>

<verification>
1. Open `client/index.html` in a browser via `cd client && npm run dev` → DOM `<head>` shows three Google Fonts links (preconnect x2, stylesheet x1).
2. In any existing page (e.g., Dashboard), inspect a heading element in DevTools → computed `font-family` shows `Inter` after fonts load.
3. Run `cd client && npx tailwindcss --help` works (config still valid JS).
4. Run `cd client && npm run build` succeeds — no Tailwind config errors, no broken existing pages (existing `brand-*` classes still resolve).
</verification>

<success_criteria>
- `client/index.html` head contains Google Fonts preconnect + stylesheet for Inter (300-800) + Manrope (500-900). (DS-01)
- `client/tailwind.config.js` exports a `meridian` color namespace with all 14 tokens at the exact hex values from `newui/meridian/shared.jsx`. (DS-02)
- `client/tailwind.config.js` exports `font-display` (Manrope-first) and `shadow-meridian-card` utilities. (DS-03)
- All existing `brand-*` / `kanan-*` / `shadow-card` / animation keys remain intact. (Backwards compat — CONTEXT.md LOCKED)
- `npm run build` passes (no broken existing pages).
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-extraction-foundation/01-01-SUMMARY.md` documenting:
- Exact lines added to `client/index.html`
- Exact `meridian` keys / `font-display` / `shadow-meridian-card` added to `client/tailwind.config.js`
- Decision recorded: chose `font-display` over `font-manrope` (idiomatic; future-proof if a different display face replaces Manrope).
- Verification: list of utility classes proven to resolve (`bg-meridian-navy`, `text-meridian-text`, `shadow-meridian-card`, `font-display`).
- Confirmation that no existing `brand-*` / `kanan-*` keys were removed.
</output>
