---
phase: 4
plan: 1
status: complete
---

# Plan 04-01 - Agent History Backend API

## Goal

Provide backend endpoints for summary and paginated agent visit history.

## Tasks

- Add `getAgentHistory` controller.
- Add `getAgentVisits` controller.
- Add role gate: `home_visit` receives 403, `accounts` receives read-only data.
- Add route wiring before generic `/:id`.
- Verify server test suite.

## Verification

- `npm test --prefix server`
