# Plan 07-01 - Visits Module Meridian UI

## Scope

Restyle the visit list, visit form, and visit detail modal around the Meridian baseline.

## Tasks

1. Update `VisitsList.jsx` to use Meridian card/table wrappers and add a working CSV export button.
2. Keep search, status filters, advanced filters, New Visit routing, edit, delete, unlock, and detail open actions working.
3. Update `NewVisit.jsx` form chrome to flat Meridian cards and remove decorative mesh/blob treatment.
4. Update `StepIndicator.jsx` so current step is gold and completed steps are green.
5. Rebuild `VisitDetailModal.jsx` as a full-screen modal with sticky header and footer while preserving all detail sections.
6. Verify production build.

## Success Criteria Mapping

- UI-V-01: `VisitsList.jsx` has Meridian search/filter/table/card wrappers, status pills, CSV export, and New Visit action.
- UI-V-02: `NewVisit.jsx` and `StepIndicator.jsx` use Meridian chrome with gold current step and green completed steps.
- UI-V-03: `VisitDetailModal.jsx` is full-screen with sticky header/footer and scrollable content.
