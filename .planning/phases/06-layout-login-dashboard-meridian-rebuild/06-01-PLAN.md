# Plan 06-01 - Meridian Shell, Login, and Dashboard Entry

## Scope

Rebuild the app shell, login screen, and dashboard baseline without changing route ownership or server contracts.

## Tasks

1. Update `Layout.jsx` to a 236px Meridian navy desktop sidebar, 58px sticky topbar, tablet icon rail, and phone drawer.
2. Replace `Login.jsx` with a navy-left/form-right split pane while preserving `react-hook-form` validation and auth flow.
3. Normalize shared component utilities in `index.css` away from glass backgrounds, oversized radii, and negative heading tracking.
4. Tighten dashboard header, loading skeletons, tooltip chrome, and KPI cards onto the Meridian baseline.
5. Verify production build.

## Success Criteria Mapping

- UI-S-01: `Layout.jsx` uses the navy/gold shell, topbar, and shared `NotifBell`.
- UI-S-02: `Login.jsx` renders split-pane navy/form layout.
- UI-S-03: `Dashboard.jsx` keeps KPI cards, chart, status breakdown, recent visits, and open action item widgets.
- UI-S-04: Desktop full sidebar, tablet icon rail, and phone drawer are implemented without changing route content layout unexpectedly.
